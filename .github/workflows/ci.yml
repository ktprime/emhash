name: CI

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'images/**'
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.os }} / ${{ matrix.compiler }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            compiler: g++
          - os: ubuntu-latest
            compiler: clang++
          - os: macos-latest
            compiler: g++
          - os: macos-latest
            compiler: clang++

    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          package="${{ matrix.compiler }}"
          if [[ "$package" == "clang++" ]]; then
            package="clang"
          fi
          sudo apt-get update && sudo apt-get install -y "$package"

      - name: Install GCC on macOS (real gcc, not clang)
        if: runner.os == 'macOS' && matrix.compiler == 'g++'
        run: |
          brew install gcc
          # Dynamically find the installed g++ version (avoid hardcoding)
          BREW_PREFIX=$(brew --prefix)
          GPP_BIN=$(ls "$BREW_PREFIX/bin/g++-"* 2>/dev/null | head -1)
          if [[ -n "$GPP_BIN" ]]; then
            ln -sf "$GPP_BIN" "$BREW_PREFIX/bin/g++"
          fi
          echo "$BREW_PREFIX/bin" >> $GITHUB_PATH
          g++ --version | head -1

      - name: Build verify tests
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -O2 -I../include verify/test_all_maps.cpp -o test_verify
          ${{ matrix.compiler }} -std=c++17 -O2 -I../include verify/test_extreme.cpp -o test_extreme
          ${{ matrix.compiler }} -std=c++17 -O2 -I../include verify/test_hashset_allocator.cpp -o test_hashset_allocator
        shell: bash

      - name: Build debug tests
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -g -O0 -I../include debug/debug_all_maps.cpp -o test_debug
        shell: bash

      - name: Build attack tests
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -O2 -DEMH_SAFE_PSL -I../include attack/hash_attack_all.cpp -o test_attack
        shell: bash

      - name: Build stress tests
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -O2 -I../include stress/stress_all_maps.cpp -o test_stress
        shell: bash

      - name: Run verify tests
        run: |
          cd tests
          ./test_verify
          ./test_extreme
          ./test_hashset_allocator
        shell: bash

      - name: Run debug tests
        run: cd tests && ./test_debug
        shell: bash

      - name: Strict warnings (C++17)
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -O2 -Wall -Wextra -Wpedantic -Wshadow -Wnon-virtual-dtor -Wno-unused-parameter -Werror -I../include verify/test_all_maps.cpp -o test_warnings17
        shell: bash

      - name: Strict warnings (C++20)
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow -Wnon-virtual-dtor -Wno-unused-parameter -Werror -I../include verify/test_all_maps.cpp -o test_warnings20
        shell: bash

  # Multi-version compiler compatibility check (uses default compiler versions)
  compat:
    name: Compat / ${{ matrix.compiler }} / C++${{ matrix.std }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [g++, clang++]
        std: [17, 20]

    steps:
      - uses: actions/checkout@v6

      - name: Install compiler
        run: |
          sudo apt-get update
          package="${{ matrix.compiler }}"
          if [[ "$package" == "clang++" ]]; then
            package="clang"
          fi
          sudo apt-get install -y "$package"

      - name: Compile verify tests
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++${{ matrix.std }} -O2 -I../include verify/test_all_maps.cpp -o test_verify_compat
        shell: bash

      - name: Run verify tests
        run: cd tests && ./test_verify_compat
        shell: bash

  asan:
    name: ASan / UBSan
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [g++, clang++]

    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          package="${{ matrix.compiler }}"
          if [[ "$package" == "clang++" ]]; then
            package="clang"
          fi
          sudo apt-get update && sudo apt-get install -y "$package"

      - name: Build with ASan
        run: |
          cd tests
          ${{ matrix.compiler }} -std=c++17 -O1 -fsanitize=address,undefined -fno-omit-frame-pointer -I../include verify/test_all_maps.cpp -o test_verify_asan
          ${{ matrix.compiler }} -std=c++17 -O1 -fsanitize=address,undefined -fno-omit-frame-pointer -I../include stress/stress_fix.cpp -o test_stress_asan
        shell: bash

      - name: Run with ASan
        run: |
          cd tests
          ./test_verify_asan
          ./test_stress_asan
        shell: bash

  cmake:
    name: CMake / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v6

      - name: Configure
        run: cmake -B build -S tests
        shell: bash

      - name: Build
        run: cmake --build build --target test_verify test_debug test_attack test_stress quick_test8 test_emilib_comprehensive
        shell: bash

      - name: Run quick tests
        run: |
          cd build
          ./test_verify || true
          ./test_debug || true
        shell: bash

  cmake-install:
    name: CMake install / find_package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Configure & Install
        run: |
          cmake -B build -DCMAKE_INSTALL_PREFIX=/tmp/emhash-install
          cmake --build build
          cmake --install build
        shell: bash

      - name: Verify installed files
        run: |
          echo "=== Installed headers ==="
          find /tmp/emhash-install -name '*.hpp' -o -name '*.h' | sort
          echo "=== CMake config files ==="
          find /tmp/emhash-install -name '*.cmake' | sort
          # Verify key files exist
          test -f /tmp/emhash-install/include/emhash/hash_table7.hpp
          test -f /tmp/emhash-install/include/emilib/emihmap1.hpp
          test -f /tmp/emhash-install/lib/cmake/emhash/emhashConfig.cmake
          test -f /tmp/emhash-install/lib/cmake/emhash/emhashConfigVersion.cmake
          echo "All install checks passed"
        shell: bash

      - name: Test find_package
        run: |
          mkdir -p /tmp/test_find_package && cd /tmp/test_find_package
          cat > CMakeLists.txt <<'EOF'
          cmake_minimum_required(VERSION 3.16)
          project(test_find_package LANGUAGES CXX)
          find_package(emhash REQUIRED CONFIG PATHS /tmp/emhash-install/lib/cmake/emhash NO_DEFAULT_PATH)
          add_executable(test_app test.cpp)
          target_link_libraries(test_app PRIVATE emhash::emhash)
          EOF
          cat > test.cpp <<'EOF'
          #include <emhash/hash_table7.hpp>
          int main() {
              emhash7::HashMap<int,int> m;
              m[1] = 2;
              return m.size() == 1 ? 0 : 1;
          }
          EOF
          cmake -B build -DCMAKE_PREFIX_PATH=/tmp/emhash-install
          cmake --build build
          ./build/test_app
          echo "find_package smoke test passed"
        shell: bash

  windows-msvc:
    name: Windows / MSVC / C++${{ matrix.std }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        std: [17, 20]
    steps:
      - uses: actions/checkout@v6

      - name: Configure (CMake)
        run: cmake -B build -S tests
        shell: bash

      - name: Build
        run: cmake --build build --target test_verify test_debug test_attack --config Release
        shell: bash

      - name: Run tests
        run: |
          ./build/Release/test_verify.exe
          ./build/Release/test_debug.exe
        shell: bash
