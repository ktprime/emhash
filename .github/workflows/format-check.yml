name: Format Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  clang-format:
    name: clang-format (LLVM style)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install clang-format-20
        run: |
          # clang-format-20 is not in Ubuntu 24.04 default repos; add LLVM apt repo
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | \
            sudo gpg --dearmor --yes -o /usr/share/keyrings/apt.llvm.org.gpg
          echo "deb [signed-by=/usr/share/keyrings/apt.llvm.org.gpg] http://apt.llvm.org/noble/ llvm-toolchain-noble-20 main" | \
            sudo tee /etc/apt/sources.list.d/llvm.list
          sudo apt-get update
          sudo apt-get install -y clang-format-20

      - name: Check formatting
        run: |
          # Check our own headers and tests
          # Exclude third-party sse2neon.h, doctest.h and build directories
          find include tests \
            -type f \( -name '*.hpp' -o -name '*.h' -o -name '*.cpp' \) \
            -not -name 'sse2neon.h' \
            -not -name 'doctest.h' \
            -not -path '*/build*' \
            -print0 | xargs -0 clang-format-20 --dry-run --Werror

      - name: Show diff (if any)
        if: failure()
        run: |
          find include tests \
            -type f \( -name '*.hpp' -o -name '*.h' -o -name '*.cpp' \) \
            -not -name 'sse2neon.h' \
            -not -name 'doctest.h' \
            -not -path '*/build*' \
            -print0 | xargs -0 clang-format-20 --dry-run
